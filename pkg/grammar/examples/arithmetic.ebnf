// Arithmetic Expression Grammar
// Demonstrates optional, repetition, and grouping

Expr = Term { ( "+" | "-" ) Term } ;

Term = Factor { ( "*" | "/" ) Factor } ;

Factor = Number | "(" Expr ")" ;

Number = [0-9]+ ;
