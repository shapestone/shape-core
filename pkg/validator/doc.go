// Package validator provides a schema validation framework for Abstract Syntax
// Tree (AST) structures. It includes a type registry for custom types (UUID,
// Email, etc.), a function registry for validation functions, multi-error
// collection, and rich error formatting with colored terminal output, plain
// text, and JSON output modes.
package validator
