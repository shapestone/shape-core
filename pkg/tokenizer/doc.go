// Package tokenizer provides a reusable tokenization framework for building
// custom parsers and domain-specific languages (DSLs) in Go. It includes a
// stream-based character reader with position tracking, built-in matchers for
// common token types (string, regex, whitespace, numbers), and an extensible
// interface for creating custom matchers.
package tokenizer
